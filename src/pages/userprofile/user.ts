export class User { 
     userKey: string; //uid du users
     nom: string;  
     prenom: string;  
     adresse: string; 
     ville: string; 
     noPostal: string;   
     noTel: number;
     email: string;

 //  constructor(values: Object = {}) {
 //   Object.assign(this, values);
 // }
} 